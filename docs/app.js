const copy = {
  tr: {
    pageTitle: "Songleton for macOS", pageDescription: "Songleton, Spotify ve Apple Music'i menü çubuğundan, ekran kenarlarından ve Ambient Mode'dan kontrol eden yerel macOS uygulaması.", primaryNav: "Ana menü", languageLabel: "Dil",
    skip: "Tanıtıma geç", navTour: "Nasıl çalışır", navFeatures: "Özellikler", navPrivacy: "Gizlilik", download: "İndir",
    heroEyebrow: "macOS 14+ için yerel müzik yardımcısı", heroTitle: "Müziğin.<br><span>Bir hareket uzağında.</span>", heroText: "Spotify ve Apple Music'i menü çubuğundan kontrol et. İmleci ekran kenarında beklet. Müziği tam ekrana al. Oynatıcıya gidip gelme işi 2012'de kalabilirdi.", downloadFree: "Ücretsiz indir", watchTour: "Canlı turu izle", openSource: "Açık kaynak", localFirst: "Yerel öncelikli", chipEdges: "Ekran kenarları", chipLyrics: "Canlı sözler",
    tourKicker: "Adım adım ürün turu", tourTitle: "Bir Mac. Bir imleç. Gereksiz pencere yok.", tourLead: "Bir kez kaydır. Her hareket seni doğrudan sonraki Songleton özelliğine götürsün.", tourControls: "Tur adımları", previousStep: "Önceki adım", nextStep: "Sonraki adım", menuFile: "Dosya", menuEdit: "Düzen", menuView: "Görüntü", desktopHello: "İyi geceler.", desktopFocus: "Müzik açık. Odak bozulmadı.", desktopNote: "Bir sonraki şarkı için sağ kenarı dene.", nowPlaying: "ŞİMDİ ÇALIYOR", scripted: "Kurgulu tanıtım",
    settingsGeneral: "Genel", settingsGestures: "Hareketler", settingsHud: "Bildirim", settingsAccess: "Erişim", settingsOverline: "SONGLETON AYARLARI", settingsTitle: "Tam senin kadar kontrolcü.", settingLaunch: "Girişte başlat", settingLaunchSub: "Mac açıldığında sessizce hazır ol.", settingNotice: "Yeni şarkı bildirimi", settingNoticeSub: "Sekiz ekran konumundan birini seç.", settingAccessName: "Spotify ve Müzik erişimi", settingAccessSub: "Açık oynatıcıyı oku ve kontrol et.", granted: "Verildi", settingLanguage: "Dil", settingLanguageSub: "Türkçe, English veya Sistem.",
    step1Title: "Önce menü çubuğuna yerleşir.", step1Text: "Şarkı, önceki, sonraki ve oynat/durdur. Oynatıcı penceresini aramak yok.", scrollHint: "Bir kez kaydır. Sonrasını biz hallederiz ↓",
    step2Title: "Üstüne gel. Kontrol odası açılsın.", step2Text: "Kapak görseli, karıştırma, yineleme, ses ve Ambient. Uygulamadaki panelin aynısı, gereksiz kalabalık olmadan.",
    step3Title: "Tıklayınca müzik durur. Şaşırtıcı teknoloji.", step3Text: "Şarkı alanına tek tıkla oynat veya duraklat. Paneldeki diğer kontroller de gerçekten çalışır.",
    step4Title: "Sağ tık. Ambient Mode.", step4Text: "Menü barındaki şarkıya sağ tıkla. O an çalan parça bütün ekranı alır.", rightClick: "Sağ tık gösteriliyor",
    step5Title: "Sol kenar, önceki şarkı.", step5Text: "İmleci kenarda beklet. Halka dolmadan ayrılırsan iptal, dolarsa önceki. Kazara şarkı katliamı yok.",
    step6Title: "Sağ kenar, sonraki şarkı.", step6Text: "Aynı kas hafızası, diğer yön. Şarkı değişimi menü barına ve bildirime akar.",
    step7Title: "Üst kenar, duraklat.", step7Text: "Bir kez beklet, müzik dursun. Halka dolarken ne olacağını görürsün.",
    step8Title: "Bir daha vur. Devam etsin.", step8Text: "Müzik geri gelir, masaüstü de ritmi çok hafif hisseder. Monitörü masadan atmıyoruz.",
    step9Title: "Sesi her yerden sürükle.", step9Text: "İki fare tuşu veya Command + Option. Dikey sürükle, ince ses göstergesi elini takip etsin.",
    step10Title: "Yeni şarkı geldi. Odağın gitmedi.", step10Text: "Sekiz konumdan birinde açılan bildirim klavye odağını çalmaz. Nadir görülen terbiyeli bildirim.",
    step11Title: "Canlı sözler, üç ayrı sahne.", step11Text: "Vinyl, Cassette ve Glass arasında geç. Senkron sözler, sarma, uyku zamanlayıcısı ve klavye kısayolları aynı ekranda.",
    step12Title: "Son sözü ayarlar söyler.", step12Text: "Hareket grupları, bekleme süresi, bildirim konumu, söz zamanlaması, menü çubuğu ve dil. Ayar var, tesisat dersi yok.",
    detailsKicker: "Geri kalan ince işler", detailsTitle: "Küçük görünür. İçeride epey iş döner.", featureHudTitle: "Sekiz konumlu şarkı bildirimi", featureHudText: "İmlecin bulunduğu ekranda açılır, en yakın kenardan girer, uygulama odağını çalmaz.", featureAmbientTitle: "Üç Ambient teması", featureAmbientText: "Vinyl, Cassette ve Glass. Dekor değil, gerçek oynatma durumu.", featureLyricsTitle: "Senkron sözler", featureLyricsText: "LRC zamanlaması, düz metin desteği ve ayarlanabilir eşzamanlama.", featureKeysTitle: "Klavye de unutulmadı", featureKeysText: "Ambient açıkken oynatma, şarkı, ses, söz ve tema kısayolları.", featurePrivacyTitle: "Dinlediğin şey iş modeli değil.", featurePrivacyText: "Hesap, analiz ve reklam yok. Kontrol Mac'inde kalır. Söz araması yalnızca eşleşme için gereken parça bilgisini kullanır.", readCode: "Kodu oku ↗",
    faqTitle: "İşe yarayan sorular.", faqAppsQ: "Hangi uygulamalar destekleniyor?", faqAppsA: "Spotify ve Apple Music masaüstü uygulamaları. Songleton ayrı bir müzik kütüphanesi kurmaz.", faqPermissionQ: "Neden oynatıcı erişimi gerekiyor?", faqPermissionA: "macOS, Spotify ve Music'e gönderilen komutları Otomasyon izniyle korur. Şarkıyı okumak ve oynatma kontrollerini kullanmak için bu izin gerekir. Erişilebilirlik izni yalnızca ekran kenarı hareketleri için kullanılır.", faqPrivacyQ: "Dinleme geçmişini yüklüyor mu?", faqPrivacyA: "Hayır. Songleton hesabı, analiz yazılımı veya dinleme geçmişi yüklemesi yok.", faqBuildQ: "Kendim derleyebilir miyim?", faqBuildA: "Evet. Proje MIT lisanslı Swift ve SwiftUI kodundan oluşuyor. Xcode yüklü bir Mac'te depoyu klonlayıp make çalıştır.",
    ctaTitle: "Müzik zaten açık.<br>Yakınında dursun.", ctaText: "macOS 14 ve sonrası için ücretsiz, yerel ve açık kaynak.", downloadSongleton: "Songleton'ı indir", footer: "Swift ile macOS için yapıldı. MIT License."
  },
  en: {
    pageTitle: "Songleton for macOS", pageDescription: "Songleton is a native macOS companion for controlling Spotify and Apple Music from the menu bar, screen edges, and Ambient Mode.", primaryNav: "Primary navigation", languageLabel: "Language",
    skip: "Skip to the tour", navTour: "How it works", navFeatures: "Features", navPrivacy: "Privacy", download: "Download",
    heroEyebrow: "Native music companion for macOS 14+", heroTitle: "Your music.<br><span>One move away.</span>", heroText: "Control Spotify and Apple Music from the menu bar. Hold the pointer at a screen edge. Give the music the whole display. App switching could have stayed in 2012.", downloadFree: "Download free", watchTour: "Watch the live tour", openSource: "Open source", localFirst: "Local first", chipEdges: "Screen edges", chipLyrics: "Live lyrics",
    tourKicker: "A step-by-step product tour", tourTitle: "One Mac. One cursor. No pointless windows.", tourLead: "Scroll once. Each gesture lands directly on the next Songleton feature.", tourControls: "Tour steps", previousStep: "Previous step", nextStep: "Next step", menuFile: "File", menuEdit: "Edit", menuView: "View", desktopHello: "Good evening.", desktopFocus: "Music on. Focus intact.", desktopNote: "Try the right edge for the next track.", nowPlaying: "NOW PLAYING", scripted: "Scripted demo",
    settingsGeneral: "General", settingsGestures: "Gestures", settingsHud: "Notification", settingsAccess: "Access", settingsOverline: "SONGLETON SETTINGS", settingsTitle: "Exactly as controlling as you are.", settingLaunch: "Launch at login", settingLaunchSub: "Be ready when your Mac starts.", settingNotice: "New-track notification", settingNoticeSub: "Choose one of eight screen positions.", settingAccessName: "Spotify & Music access", settingAccessSub: "Read and control the open player.", granted: "Granted", settingLanguage: "Language", settingLanguageSub: "Türkçe, English, or System.",
    step1Title: "First, it settles into the menu bar.", step1Text: "Track, previous, next, and play or pause. No hunting for the player window.", scrollHint: "One scroll. We handle the rest ↓",
    step2Title: "Hover. The control room opens.", step2Text: "Artwork, shuffle, repeat, volume, and Ambient. The native panel hierarchy you sent, minus the pointless playlist graveyard.",
    step3Title: "Click it. The music pauses. Wild.", step3Text: "One click on the track toggles playback. Every control in the panel is live too.",
    step4Title: "Right-click. Ambient Mode.", step4Text: "Right-click the menu bar track. The current song takes the whole display.", rightClick: "Right-click being demonstrated",
    step5Title: "Left edge, previous track.", step5Text: "Hold the pointer at the edge. Leave before the ring fills to cancel. Let it finish to go back. No accidental track massacre.",
    step6Title: "Right edge, next track.", step6Text: "The same muscle memory, the other direction. The change flows into the menu bar and notification HUD.",
    step7Title: "Top edge, pause.", step7Text: "Hold once and the music stops. The ring shows exactly when it will commit.",
    step8Title: "Hit it again. Carry on.", step8Text: "The music returns and the desktop feels the beat, gently. We are not throwing your monitor off the desk.",
    step9Title: "Drag volume anywhere.", step9Text: "Both mouse buttons or Command + Option. Drag vertically and the slim HUD follows your hand.",
    step10Title: "New track. Focus stayed put.", step10Text: "The non-activating HUD enters from one of eight positions without stealing keyboard focus. A notification with manners.",
    step11Title: "Live lyrics, three scenes.", step11Text: "Move through Vinyl, Cassette, and Glass. Synced lyrics, seek, sleep timer, and keyboard controls stay in one place.",
    step12Title: "Settings get the last word.", step12Text: "Gesture groups, hold time, HUD placement, lyric offset, menu bar, and Türkçe/English. We do not sell plumbing called Automation. We call it player access.",
    detailsKicker: "The quieter details", detailsTitle: "Looks small. Does quite a lot.", featureHudTitle: "Eight-position track HUD", featureHudText: "Appears on the display under the pointer, enters from the nearest edge, and never steals app focus.", featureAmbientTitle: "Three Ambient themes", featureAmbientText: "Vinyl, Cassette, and Glass. Real playback state, not decorative theater.", featureLyricsTitle: "Synced lyrics", featureLyricsText: "LRC timing, plain-text fallback, and an adjustable offset.", featureKeysTitle: "Keyboard included", featureKeysText: "Playback, track, volume, lyrics, and theme shortcuts while Ambient is open.", featurePrivacyTitle: "Your listening is not the business model.", featurePrivacyText: "No account, analytics, or ads. Control stays on your Mac. Lyrics lookup uses only the track data required to find a match.", readCode: "Read the code ↗",
    faqTitle: "The useful questions.", faqAppsQ: "Which apps are supported?", faqAppsA: "The Spotify and Apple Music desktop apps. Songleton does not build a second music library.", faqPermissionQ: "Why does player access exist?", faqPermissionA: "macOS protects Apple Events sent to Spotify and Music behind Automation permission. It is required to read the track and run play, pause, next, and volume commands. Accessibility is used only for global gestures.", faqPrivacyQ: "Does it upload listening history?", faqPrivacyA: "No. There is no Songleton account, analytics SDK, or listening-history upload.", faqBuildQ: "Can I build it myself?", faqBuildA: "Yes. It is an MIT-licensed Swift and SwiftUI project. Clone it and run make on a Mac with Xcode installed.",
    ctaTitle: "The music is already playing.<br>Keep it close.", ctaText: "Free, native, and open source for macOS 14 and later.", downloadSongleton: "Download Songleton", footer: "Built with Swift for macOS. MIT License."
  }
};

const demo = document.querySelector("#mac-demo");
const cursor = demo?.querySelector(".scripted-cursor");
const steps = [...document.querySelectorAll(".tour-step")];
const tourStage = document.querySelector(".tour-stage-wrap");
const previousStepButton = document.querySelector(".tour-previous");
const nextStepButton = document.querySelector(".tour-next");
const currentStepLabel = document.querySelector(".tour-counter b");
const totalStepLabel = document.querySelector(".tour-counter span");
const statusTitle = demo?.querySelector(".status-title");
const statusPlay = demo?.querySelector(".status-play");
const panelTrack = demo?.querySelector(".panel-track");
const panelArtist = demo?.querySelector(".panel-artist");
const panelPlay = demo?.querySelector(".panel-play");
const ambientTrack = demo?.querySelector(".ambient-track");
const ambientArtist = demo?.querySelector(".ambient-artist");
const ambientPlay = demo?.querySelector(".ambient-play");

// The macOS scene is a scripted visual, not a second set of controls.
// Keep its button-shaped decoration out of keyboard navigation even in browsers
// that do not implement the inert attribute yet.
demo?.querySelectorAll("button").forEach((button) => {
  button.tabIndex = -1;
  button.setAttribute("aria-hidden", "true");
});
const notificationTitle = demo?.querySelector(".notification-title");
const notificationArtist = demo?.querySelector(".notification-artist");
const ambientModeName = demo?.querySelector(".ambient-mode-name");
const menuLanguage = demo?.querySelector(".menu-lang");
const menuClock = demo?.querySelector(".menu-clock");

const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));
const mix = (from, to, progress) => from + (to - from) * progress;
const parsePoints = (step) => step.dataset.cursor.split(",").map(Number);
const minimumJerk = (value) => value ** 3 * (10 + value * (-15 + value * 6));
let rafPending = false;
let activeState = "";
let lastBeat = -1;
let activeStepIndex = 0;
let stepScrollLocked = false;
let accumulatedWheelDelta = 0;
let unlockStepTimer;
let cursorFrame;
let cursorClickTimer;
let cursorReleaseTimer;
let cursorPauseTimer;
let cursorPosition = { x: 54, y: 4 };
let interactionProgress = 1;

if (totalStepLabel) totalStepLabel.textContent = String(steps.length).padStart(2, "0");

function syncTourControls(index) {
  activeStepIndex = index;
  if (currentStepLabel) currentStepLabel.textContent = String(index + 1).padStart(2, "0");
  if (previousStepButton) previousStepButton.disabled = index === 0;
  if (nextStepButton) nextStepButton.disabled = index === steps.length - 1;
}

function goToStep(index) {
  const target = steps[index];
  if (!target) return false;

  stepScrollLocked = true;
  accumulatedWheelDelta = 0;
  syncTourControls(index);
  target.scrollIntoView({
    behavior: window.matchMedia("(prefers-reduced-motion: reduce)").matches ? "auto" : "smooth",
    block: "center"
  });

  window.clearTimeout(unlockStepTimer);
  unlockStepTimer = window.setTimeout(() => { stepScrollLocked = false; }, 860);
  return true;
}

function setCursorPosition(x, y, tilt = -5) {
  cursorPosition = { x, y };
  demo?.style.setProperty("--cursor-x", x.toFixed(3));
  demo?.style.setProperty("--cursor-y", y.toFixed(3));
  demo?.style.setProperty("--cursor-tilt", tilt.toFixed(3));
}

function scheduleCursorClick(state, duration) {
  window.clearTimeout(cursorClickTimer);
  window.clearTimeout(cursorReleaseTimer);
  cursor?.classList.remove("clicking", "right-clicking");
  if (!cursor || !["panelplay", "ambient"].includes(state)) return;

  cursorClickTimer = window.setTimeout(() => {
    cursor.classList.add(state === "ambient" ? "right-clicking" : "clicking");
    cursorReleaseTimer = window.setTimeout(() => {
      cursor.classList.remove("clicking", "right-clicking");
    }, 210);
  }, duration + 90);
}

function moveScriptedCursor(step, index, state) {
  if (!demo || !cursor) return;
  window.cancelAnimationFrame(cursorFrame);
  window.clearTimeout(cursorClickTimer);
  window.clearTimeout(cursorReleaseTimer);
  window.clearTimeout(cursorPauseTimer);
  cursor.classList.remove("clicking", "right-clicking");

  const [authoredStartX, authoredStartY, targetX, targetY] = parsePoints(step);
  const targets = state === "resume"
    ? [{ x: authoredStartX, y: authoredStartY }, { x: targetX, y: targetY }]
    : [{ x: targetX, y: targetY }];
  const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  const legDuration = (from, to) => reducedMotion
    ? 0
    : clamp(480 + Math.hypot(to.x - from.x, to.y - from.y) * 4.2, 540, 820);
  let durationCursor = { ...cursorPosition };
  const durations = targets.map((target) => {
    const duration = legDuration(durationCursor, target);
    durationCursor = target;
    return duration;
  });
  const pauseBetweenLegs = targets.length > 1 ? 120 : 0;
  const totalDuration = durations.reduce((total, duration) => total + duration, 0) + pauseBetweenLegs;

  if (reducedMotion) {
    interactionProgress = 1;
    setCursorPosition(targetX, targetY);
    scheduleCursorClick(state, 0);
    return;
  }

  interactionProgress = 0;
  demo.dataset.cursorMotion = "moving";
  demo.dataset.cursorTarget = state;

  const runLeg = (legIndex) => {
    const start = { ...cursorPosition };
    const target = targets[legIndex];
    const dx = target.x - start.x;
    const dy = target.y - start.y;
    const distance = Math.max(Math.hypot(dx, dy), .001);
    const duration = durations[legIndex];
    const normalX = -dy / distance;
    const normalY = dx / distance;
    const bendDirection = (index + legIndex) % 2 === 0 ? 1 : -1;
    const bend = clamp(distance * .11, 2.2, 7.4) * bendDirection;
    const overshoot = clamp(distance * .018, .35, 1.15);
    const controlOne = {
      x: start.x + dx * .30 + normalX * bend,
      y: start.y + dy * .30 + normalY * bend
    };
    const controlTwo = {
      x: target.x + (dx / distance) * overshoot - normalX * bend * .18,
      y: target.y + (dy / distance) * overshoot - normalY * bend * .18
    };
    const startingTilt = clamp(-5 + normalX * 3, -8, -2);
    const targetTilt = clamp(-5 - normalX * 1.5, -7, -3);
    const startedAt = performance.now();
    const finalLeg = legIndex === targets.length - 1;

    const drawFrame = (now) => {
      const linearProgress = clamp((now - startedAt) / duration);
      const progress = minimumJerk(linearProgress);
      const inverse = 1 - progress;
      const x = inverse ** 3 * start.x
        + 3 * inverse ** 2 * progress * controlOne.x
        + 3 * inverse * progress ** 2 * controlTwo.x
        + progress ** 3 * target.x;
      const y = inverse ** 3 * start.y
        + 3 * inverse ** 2 * progress * controlOne.y
        + 3 * inverse * progress ** 2 * controlTwo.y
        + progress ** 3 * target.y;
      const tilt = mix(startingTilt, targetTilt, minimumJerk(clamp(linearProgress * 1.08)));

      setCursorPosition(x, y, tilt);
      interactionProgress = finalLeg ? minimumJerk(clamp((linearProgress - .68) / .32)) : 0;
      requestTourUpdate();

      if (linearProgress < 1) {
        cursorFrame = window.requestAnimationFrame(drawFrame);
        return;
      }

      setCursorPosition(target.x, target.y, targetTilt);
      if (!finalLeg) {
        cursorPauseTimer = window.setTimeout(() => runLeg(legIndex + 1), pauseBetweenLegs);
        return;
      }

      interactionProgress = 1;
      demo.dataset.cursorMotion = "settled";
      requestTourUpdate();
    };

    cursorFrame = window.requestAnimationFrame(drawFrame);
  };

  runLeg(0);
  scheduleCursorClick(state, totalDuration);
}

function setTrack(title, artist, playing = true) {
  [statusTitle, panelTrack, ambientTrack, notificationTitle].forEach((node) => { if (node) node.textContent = title; });
  [panelArtist, ambientArtist].forEach((node) => { if (node) node.textContent = artist; });
  if (notificationArtist) notificationArtist.textContent = `${artist} · Songleton Session`;
  const icon = playing ? "Ⅱ" : "▶";
  [statusPlay, panelPlay, ambientPlay].forEach((node) => { if (node) node.textContent = icon; });
}

function updateTour() {
  rafPending = false;
  if (!demo || !steps.length) return;
  const anchor = window.innerHeight * .56;
  let active = steps[0];
  for (const step of steps) {
    const rect = step.getBoundingClientRect();
    if (rect.top <= anchor) active = step;
    if (rect.top <= anchor && rect.bottom > anchor) { active = step; break; }
  }

  const rect = active.getBoundingClientRect();
  const activeIndex = steps.indexOf(active);
  syncTourControls(activeIndex);
  const rawProgress = clamp((anchor - rect.top) / Math.max(rect.height * .72, 1));
  demo.style.setProperty("--step-progress", rawProgress.toFixed(3));

  const state = active.dataset.state;
  if (activeState !== state) {
    activeState = state;
    demo.dataset.state = state;
    demo.classList.remove("beat");
    lastBeat = -1;
    moveScriptedCursor(active, activeIndex, state);
  }
  steps.forEach((step) => step.classList.toggle("active", step === active));

  const panelOpacity = state === "hover"
    ? clamp(rawProgress * 2.4) * interactionProgress
    : state === "panelplay"
      ? 1
      : state === "ambient"
        ? clamp(1 - Math.max(0, rawProgress - .42) * 3) * interactionProgress
        : 0;
  const ambientOpacity = state === "ambient" ? clamp((rawProgress - .43) * 3.2) * interactionProgress : state === "lyrics" ? interactionProgress : 0;
  const edgeOpacity = ["previous", "next", "pause", "resume"].includes(state) ? clamp(rawProgress * 3) * interactionProgress : 0;
  const volumeOpacity = state === "volume" ? clamp(rawProgress * 3) * interactionProgress : 0;
  const noticeOpacity = state === "notification" ? clamp((rawProgress - .18) * 3.1) * interactionProgress : 0;
  const settingsOpacity = state === "settings" ? clamp((rawProgress - .1) * 2.5) * interactionProgress : 0;
  demo.style.setProperty("--panel-opacity", panelOpacity.toFixed(3));
  demo.style.setProperty("--ambient-opacity", ambientOpacity.toFixed(3));
  demo.style.setProperty("--edge-opacity", edgeOpacity.toFixed(3));
  demo.style.setProperty("--volume-opacity", volumeOpacity.toFixed(3));
  demo.style.setProperty("--notice-opacity", noticeOpacity.toFixed(3));
  demo.style.setProperty("--settings-opacity", settingsOpacity.toFixed(3));

  if (state === "previous" && interactionProgress > .62) setTrack("After Dark", "Mr.Kitty");
  else if (state === "next" && interactionProgress > .62) setTrack("Midnight City", "M83");
  else if (state === "pause" && interactionProgress > .62) setTrack("Midnight City", "M83", false);
  else if (state === "resume") {
    setTrack("Midnight City", "M83", interactionProgress > .62);
    const beat = Math.floor(Math.max(0, interactionProgress - .62) * 8);
    if (interactionProgress > .62 && beat !== lastBeat) {
      lastBeat = beat;
      demo.classList.remove("beat");
      void demo.offsetWidth;
      demo.classList.add("beat");
    }
  } else if (!["previous", "pause"].includes(state)) setTrack("Midnight City", "M83");

  if (state === "lyrics") {
    const theme = rawProgress < .34 ? "vinyl" : rawProgress < .68 ? "cassette" : "glass";
    demo.dataset.theme = theme;
    if (ambientModeName) ambientModeName.textContent = `AMBIENT · ${theme.toUpperCase()}`;
  } else {
    demo.dataset.theme = "vinyl";
    if (ambientModeName) ambientModeName.textContent = "AMBIENT · VINYL";
  }
}

function requestTourUpdate() {
  if (rafPending) return;
  rafPending = true;
  requestAnimationFrame(updateTour);
}

window.addEventListener("scroll", requestTourUpdate, { passive: true });
window.addEventListener("resize", requestTourUpdate);

window.addEventListener("wheel", (event) => {
  if (!tourStage || !steps.length || event.ctrlKey || Math.abs(event.deltaX) > Math.abs(event.deltaY)) return;

  const stageRect = tourStage.getBoundingClientRect();
  const tourIsActive = stageRect.top < window.innerHeight * .58 && stageRect.bottom > window.innerHeight * .42;
  if (!tourIsActive) return;

  const direction = Math.sign(event.deltaY);
  const canAdvance = direction > 0 && activeStepIndex < steps.length - 1;
  const canReturn = direction < 0 && activeStepIndex > 0;
  if (!canAdvance && !canReturn) return;

  event.preventDefault();
  if (stepScrollLocked) return;

  accumulatedWheelDelta += event.deltaY;
  if (Math.abs(accumulatedWheelDelta) < 18) return;
  goToStep(activeStepIndex + Math.sign(accumulatedWheelDelta));
}, { passive: false });

previousStepButton?.addEventListener("click", () => goToStep(activeStepIndex - 1));
nextStepButton?.addEventListener("click", () => goToStep(activeStepIndex + 1));
requestTourUpdate();

function applyLanguage(language) {
  const selected = copy[language] ? language : "tr";
  document.documentElement.lang = selected;
  document.title = copy[selected].pageTitle;
  document.querySelector('meta[name="description"]')?.setAttribute("content", copy[selected].pageDescription);
  document.querySelectorAll("[data-i18n-label]").forEach((node) => {
    const value = copy[selected][node.dataset.i18nLabel];
    if (value !== undefined) node.setAttribute("aria-label", value);
  });
  document.querySelectorAll("[data-i18n]").forEach((node) => {
    const value = copy[selected][node.dataset.i18n];
    if (value === undefined) return;
    if (node.dataset.i18n === "heroTitle" || node.dataset.i18n === "ctaTitle") {
      const parts = value.split("<br>");
      const accent = document.createElement("span");
      accent.textContent = (parts[1] || "").replace(/^<span>|<\/span>$/g, "");
      node.replaceChildren(document.createTextNode(parts[0]), document.createElement("br"), accent);
    } else node.textContent = value;
  });
  document.querySelectorAll("[data-language]").forEach((button) => button.setAttribute("aria-pressed", String(button.dataset.language === selected)));
  if (menuLanguage) menuLanguage.textContent = selected.toUpperCase();
  if (menuClock) menuClock.textContent = selected === "tr" ? "Çar 01:18" : "Wed 01:18";
  try { localStorage.setItem("songleton-language", selected); } catch (_) {}
}

document.querySelectorAll("[data-language]").forEach((button) => button.addEventListener("click", () => applyLanguage(button.dataset.language)));
let savedLanguage = "tr";
try { savedLanguage = localStorage.getItem("songleton-language") || (navigator.language.toLowerCase().startsWith("tr") ? "tr" : "en"); } catch (_) {}
applyLanguage(savedLanguage);

document.querySelectorAll(".feature-card").forEach((card) => {
  card.addEventListener("pointermove", (event) => {
    const rect = card.getBoundingClientRect();
    card.style.setProperty("--pointer-x", `${event.clientX - rect.left}px`);
    card.style.setProperty("--pointer-y", `${event.clientY - rect.top}px`);
  });
});
